worker_processes auto;
pid        /tmp/nginx.pid;
error_log  /var/log/nginx/error.log warn;

events {
    worker_connections 1024;
}

http {
    include      /etc/nginx/mime.types;
    default_type application/octet-stream;

    # Hide nginx version in headers and error pages
    server_tokens off;

    # Log real visitor IP (passed by Cloudflare) rather than Cloudflare's edge IP
    log_format main '$http_cf_connecting_ip - $remote_user [$time_local] '
                    '"$request" $status $body_bytes_sent '
                    '"$http_referer" "$http_user_agent"';
    access_log /var/log/nginx/access.log main;

    sendfile    on;
    tcp_nopush  on;
    tcp_nodelay on;

    keepalive_timeout 65;

    # Gzip
    gzip            on;
    gzip_vary       on;
    gzip_proxied    any;
    gzip_comp_level 6;
    gzip_types
        text/plain
        text/css
        text/javascript
        application/javascript
        application/json
        image/svg+xml;

    # ---------------------------------------------------------------------------
    # Cloudflare-only access
    # Allow connections only from Cloudflare edge IPs.
    # Keep this list up-to-date: https://www.cloudflare.com/ips/
    # ---------------------------------------------------------------------------
    geo $not_cloudflare {
        default         1;

        # Allow loopback for Docker healthcheck
        127.0.0.1        0;

        # Cloudflare IPv4
        173.245.48.0/20  0;
        103.21.244.0/22  0;
        103.22.200.0/22  0;
        103.31.4.0/22    0;
        141.101.64.0/18  0;
        108.162.192.0/18 0;
        190.93.240.0/20  0;
        188.114.96.0/20  0;
        197.234.240.0/22 0;
        198.41.128.0/17  0;
        162.158.0.0/15   0;
        104.16.0.0/13    0;
        104.24.0.0/14    0;
        172.64.0.0/13    0;
        131.0.72.0/22    0;

        # Cloudflare IPv6
        2400:cb00::/32   0;
        2606:4700::/32   0;
        2803:f800::/32   0;
        2405:b500::/32   0;
        2405:8100::/32   0;
        2a06:98c0::/29   0;
        2c0f:f248::/32   0;
    }

    server {
        listen 80;
        server_name _;

        root  /usr/share/nginx/html;
        index index.html;

        # Block any request not originating from Cloudflare
        if ($not_cloudflare) {
            return 444;  # Drop connection silently
        }

        # Security headers
        add_header X-Content-Type-Options   "nosniff"                           always;
        add_header X-Frame-Options          "DENY"                              always;
        add_header Referrer-Policy          "strict-origin-when-cross-origin"   always;
        add_header Permissions-Policy       "geolocation=(), camera=(), microphone=()" always;
        add_header Content-Security-Policy
            "default-src 'self'; script-src 'self'; style-src 'self'; img-src 'self' data:; connect-src 'none'; font-src 'self'; object-src 'none'; base-uri 'self'; form-action 'self';"
            always;

        # Cache: no-cache for HTML (always fresh), long cache for versioned assets
        location ~* \.html$ {
            add_header Cache-Control "no-cache, must-revalidate" always;
            # Re-add security headers (they don't inherit inside location blocks)
            add_header X-Content-Type-Options   "nosniff"                         always;
            add_header X-Frame-Options          "DENY"                            always;
            add_header Referrer-Policy          "strict-origin-when-cross-origin" always;
            add_header Content-Security-Policy
                "default-src 'self'; script-src 'self'; style-src 'self'; img-src 'self' data:; connect-src 'none'; font-src 'self'; object-src 'none'; base-uri 'self'; form-action 'self';"
                always;
        }

        location ~* \.(css|js)$ {
            add_header Cache-Control "public, max-age=86400, stale-while-revalidate=3600" always;
        }

        location / {
            try_files $uri $uri/ /index.html;
        }

        # Block dotfiles
        location ~ /\. {
            deny all;
        }
    }
}
