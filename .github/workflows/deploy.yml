name: Deploy

on:
  push:
    branches: [main]
  workflow_dispatch:   # allow manual trigger from GitHub UI

jobs:
  deploy:
    runs-on: [self-hosted, hetzner-de-1]

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Pull latest nginx image
        run: docker compose -p doesmycarryonfit pull

      - name: Deploy
        run: docker compose -p doesmycarryonfit up -d --force-recreate

      - name: Verify
        run: |
          sleep 3
          status=$(docker inspect --format='{{.State.Health.Status}}' doesmycarryonfit-web-1 2>/dev/null || echo "no-healthcheck")
          echo "Container health: $status"
          docker compose -p doesmycarryonfit ps
